server:
  port: 8072
  
spring:
  application:
    name: "configserver"
  profiles:
    active: "git"
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/kshan31/configRepo"
default-label: "main"
server:
  port: 8072
